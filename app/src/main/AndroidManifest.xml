<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    Declares app permissions, the splash screen theme, and all activities.
    RECORD_AUDIO is required for the voice-recitation / speech-to-text feature.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Voice recognition for Quranic recitation verification -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Keep CPU awake for long recitation sessions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Schedule daily review reminders -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".QuranApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuranMemorization.SplashScreen"
        android:localeConfig="@xml/locales_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Daily reminder notification receiver -->
        <receiver
            android:name=".data.local.ReminderReceiver"
            android:exported="false" />

    </application>

</manifest>
